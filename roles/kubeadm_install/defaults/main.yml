---
# Role: kubeadm_install
# Defaults for joining a fresh node as a worker. The OS-prep version pins
# (k8s_join_minor/k8s_join_patch/k8s_pause_image) now live in
# roles/k8s_node_prereqs/defaults/main.yml, which this role imports first —
# see that file for the authoritative fallbacks and group_vars/new_workers.yml
# for the real values used on actual joins.

# Set by play 1 of playbooks/join_nodes.yml (kubeadm join ... --token ... --hash).
kubeadm_join_cmd: ""